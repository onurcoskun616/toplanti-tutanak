fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy[asyncio]==2.0.30
aiosqlite==0.22.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-multipart==0.0.9
# httpx: required by Starlette's TestClient (see tests/conftest.py).
httpx==0.27.0
pytest==8.2.2
# ASR (app/asr.py). CPU-only via ctranslate2 — no torch/GPU needed. Model
# weights (default "small", Turkish) download from Hugging Face on first use
# unless pre-baked into the image. The import is guarded in app/asr.py, so a
# deploy without this package still boots — the audio-chunk endpoint just
# answers 503.
faster-whisper==1.0.3
# faster-whisper's utils.py imports requests directly, but its huggingface-hub
# dependency doesn't reliably pull it transitively — pin it explicitly so a
# fresh install doesn't hit "ModuleNotFoundError: No module named 'requests'".
requests
